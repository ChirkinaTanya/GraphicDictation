<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/instructionBtn"
        android:layout_width="190dp"
        android:layout_height="42dp"
        android:layout_marginTop="32dp"
        android:text="Инструкция"
        android:onClick="openInstruction"
        app:layout_constraintBottom_toTopOf="@+id/settingsBtn"
        app:layout_constraintEnd_toEndOf="@+id/settingsBtn"
        app:layout_constraintHorizontal_bias="0.517"
        app:layout_constraintStart_toStartOf="@+id/settingsBtn"
        app:layout_constraintTop_toBottomOf="@+id/playBtn"
        app:layout_constraintVertical_bias="0.0" />

    <Button
        android:id="@+id/settingsBtn"
        android:layout_width="190dp"
        android:layout_height="42dp"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="280dp"
        android:text="Настройки"
        android:onClick="openSettings"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/instructionBtn"/>

    <Button
        android:id="@+id/playBtn"
        android:layout_width="190dp"
        android:layout_height="42dp"
        android:layout_marginTop="252dp"
        android:onClick="openLevels"
        android:text="Играть"
        app:layout_constraintBottom_toTopOf="@+id/instructionBtn"
        app:layout_constraintEnd_toEndOf="@+id/instructionBtn"
        app:layout_constraintStart_toStartOf="@+id/instructionBtn"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>